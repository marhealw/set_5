# HyperLogLog: исследование точности потокового алгоритма

Репозиторий содержит реализацию и исследование алгоритма HyperLogLog для оценки F0 — количества уникальных элементов в потоке.

## Структура
- `src/stream/` — генератор потоков строк `RandomStreamGen`
- `src/hash/` — генератор хеш-функции `HashFuncGen`, отображение строк в `uint32_t`
- `src/hll/` — стандартный HyperLogLog
- `src/experiments/` — эксперименты: точный F0, оценка Nt, сбор статистик и CSV
- `src/analysis/` — построение графиков и анализ результатов
- `data/` — результаты прогонов (CSV)
- `report/` — отчет и графики

## Сборка
```bash
cmake -S . -B build
cmake --build build -j
